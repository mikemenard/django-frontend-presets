============
Installation
============

At the command line::

    $ easy_install django-frontend-presets

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-frontend-presets
    $ pip install django-frontend-presets
